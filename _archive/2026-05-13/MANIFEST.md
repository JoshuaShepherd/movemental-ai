# Repo Archive Manifest — 2026-05-13

Generated by the `$repo-cleanup` skill on 2026-05-13 in branch
`slice/S00-frontend-cleanup-2026-05-13` (continues from the frontend-cleanup
commit `9898856`).

**Source report:** [`../../_docs/_build/repo-cleanup-2026-05-13.md`](../../_docs/_build/repo-cleanup-2026-05-13.md)

**Sibling archive:** [`../../src/components/_archive/2026-05-13/`](../../src/components/_archive/2026-05-13/) — the frontend-cleanup pass that ran first (100 component files).

## Moves (4 files)

Scope tightened from 9 → 4 during Phase 4 pre-flight when `grep` surfaced that `docs/html/scenius-network-v2/` is a **generated artifact** with active generator scripts (`scripts/sync-movement-voices-html-data.ts` and `scripts/generate-movement-voices-html-app-js.py`). Archiving it would have orphaned the generators on their next run. Backed off and restored that directory.

| From | To | Reason |
|---|---|---|
| `docs/html/_write_test.txt` | `docs-html/_write_test.txt` | Zero-byte test marker from 2026-05-11 write-permissions probe |
| `docs/html/homepage-concept-modern-v2/index.html` | `docs-html/homepage-concept-modern-v2/index.html` | Explicit `-v2` Stitch mockup; React equivalent shipped in `src/components/sections-mock/home/`. Only remaining reference is a single JSDoc comment in `home-concept-modern-page-content.tsx:205` (informational, no runtime impact). |
| `docs/html/homepage-concept-modern-v2/script.js` | `docs-html/homepage-concept-modern-v2/script.js` | (companion) |
| `docs/html/homepage-concept-modern-v2/styles.css` | `docs-html/homepage-concept-modern-v2/styles.css` | (companion) |

## What was left in place (and why)

- **`docs/html/scenius-network-v2/`** — generated artifact, not bloat. Two active generator scripts (`sync-movement-voices-html-data.ts`, `generate-movement-voices-html-app-js.py`) write into this directory; archiving it would break them on next run.
- **`docs/html/` overall (278 remaining files)** — Stitch HTML mockup graveyard. Per `CLAUDE.md`, the canonical visual SSOT is the pinned Stitch project `2208910962065880866`, not these local HTML files. Many are likely now reference-only since the React migration is well underway, but archiving them safely requires a per-file check against the live route list. A focused follow-up pass is the right vehicle.
- **`docs/articles/sandbox/`** — proper article frontmatter (`shape: sandbox`, `series_order: N`); structured drafts, not scratch.
- **`docs/dashboard-{route-inventory,alignment-changelog}.md`** — both self-document as SSOT.
- **`public/templates/{safety,sandbox}/`** — referenced by `src/lib/sandboxlive/recipe-catalog.ts` and Stitch validation scripts.
- **17 dependencies flagged by coarse grep** — all false-positives (shadcn-bundled Radix primitives, Next peer deps, dev tools). A dedicated `knip` run would be the right tool.

## How to revert

Single commit; `git revert <sha>` restores everything.

---

## Update — third pass (same commit / branch)

Followed up the initial 4-file repo-cleanup with a focused **docs/html cleanup pass**. Source report: [`../../_docs/_build/docs-html-cleanup-2026-05-13.md`](../../_docs/_build/docs-html-cleanup-2026-05-13.md).

**Archived in this pass:** 119 files (Stitch HTML prototype mockups whose React equivalents have shipped, and small one-off mockup folders).

**Kept in `docs/html/` (164 files):**
- Generator outputs: `scenius-network-v2/`, `scenius-network-v3/`, `toolkit-text-review.html`, `books-concept-modern/`, `site-templates/`
- Read by build scripts: `deduped-megapage.{html,css,js}`, `tabbed-argument-page.html`, `argument-ids.json`
- Cited as canonical SSOT by build prompts: `home-citations-ledger.html` (design SSOT for the citations system), `site-claims-eeat-research-map.html`
- Self-contained subdirs left intact for separate review: `alan-hirsch-course-migration/` (72 files), `master-components/` (29), `course-previews/` (20), `assets/` (10), `scripts/` (2)
- `README.md` (the directory's policy doc)

**Known minor side-effect:** `scripts/build-book-reader-html.py` generates an HTML page that includes prototype-nav hyperlinks (e.g., `../homepage-concept-modern/index.html`). Those links will 404 after this archive on next regeneration. The book-reader's primary purpose (rendering the manuscript) is unaffected; only the courtesy cross-prototype nav at the top of the generated page is broken. The generator can be patched to point at archive paths if/when desired.

**Live-reference audit:** 3 `src/` JSDoc comments still mention archived paths (`home-page-content.tsx:3`, `reveal-on-scroll.tsx:15`, `movement-leaders-page-content.tsx:26`). All are documentary comments only — not load-bearing — so they remain in place.

**Verification:** `pnpm typecheck` ✓.
