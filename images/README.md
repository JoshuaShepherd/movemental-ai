# Aggregated headshots / images

Collected from a Mac-wide search for image files whose path or filename contained:

- **Brad Brisco** → `images/brad-brisco/`
- **Alan Hirsch** → `images/alan-hirsch/`
- **Josh Shepherd** → `images/josh-shepherd/` (no files found with "josh" and "shepherd" in the filename)

Deduplicated by filename; when the same filename appeared in multiple locations, the copy from this repo (`movemental-ai`) was kept.

**Counts (as of run):**

- **brad-brisco:** 28 unique images (headshots, compare/design variants, marketing assets)
- **alan-hirsch:** 171 unique images (headshots, brand marks, portraits, marketing assets)
- **josh-shepherd:** 0 (add any Josh Shepherd headshots here manually if you have them)

To re-run the aggregation (e.g. after adding more images elsewhere), run from repo root:

```bash
./aggregate-images.sh
```
