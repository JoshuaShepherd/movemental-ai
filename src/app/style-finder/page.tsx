import { StyleFinderWizard } from "@/components/style-finder/style-finder-wizard";

/**
 * Unlisted visual style discovery wizard — share via direct URL only.
 * Not linked from nav or sitemap.
 */
export default function StyleFinderPage() {
  return <StyleFinderWizard />;
}
