# Frontend Component Archive — 2026-05-13

Generated by the `frontend-cleanup` skill on 2026-05-13 in branch
`slice/S00-frontend-cleanup-2026-05-13`.

**Source report:** [`_docs/_build/frontend-cleanup-report.md`](../../../../_docs/_build/frontend-cleanup-report.md)

**Total archived:** 100 component files (originally scoped at 88 in the report;
during Phase 4 pre-flight, `pnpm typecheck` surfaced a closed 15-file dead
chain — `sections/services/*`, `editorial-stitch/stitch-glass-top-bar`, and the
12-file `nav/` cluster — that imports itself but is mathematically unreachable
from `src/app/layout.tsx`. The chain had to move together to ship a green
build, so scope expanded from 85 to 100. Independent verification: `grep`
across `src/` returns zero live importers for every file in the chain.)

**Verification at commit time:** `pnpm typecheck` ✓ · `pnpm lint` ✓ (0 errors,
64 pre-existing warnings) · `pnpm build` ✓.

## What was archived and why

### Whole folders (61 files)

| Origin | Files | Reason |
|---|---:|---|
| `src/components/marketing/` | 32 | Zero external importers; entire `knowledge-graph/`, `story/`, `ui/`, `path-shared/` subtrees orphaned |
| `src/components/path/` | 26 | PascalCase legacy stages + case studies; superseded by `pathway/` and page-level sections |
| `src/components/sections/nonprofit-funnel-new/` | 2 | Zero importers |
| `src/components/sections/resources-templates/` | 2 | Self-imports only |
| `src/components/sections/services/` | 2 | No `/services` route exists; transitively dead |
| `src/components/editorial-stitch/` (entire dir) | 9 | After archiving the 7 non-imported files, the remaining `stitch-glass-top-bar.tsx` and barrel were also dead (only the dead chain referenced them) — whole directory removed |

### Closed dead `-new` shadow tree in `sections/fragmentation-story/` (7 files)

| File | Status |
|---|---|
| `fragmentation-story-new-shell.tsx` | Top of dead tree; 0 importers |
| `fragmentation-story-new-wayfinding.tsx` | Transitively dead |
| `fragmentation-story-stage-activation-new.tsx` | Transitively dead |
| `fragmentation-story-stage-formation-new.tsx` | Transitively dead |
| `fragmentation-story-stage-integration-new.tsx` | Transitively dead |
| `fragmentation-story-stage-movement-new.tsx` | Transitively dead |
| `fragmentation-story-stage-multiplication-new.tsx` | Transitively dead |

The non-`-new` originals (`fragmentation-story-stage-activation.tsx`, etc.) remain in place — they have 2 importers each and are still live.

### Smaller graveyards (20 files)

- `sections/home/home-audience-examples-new{.tsx,-client.tsx}` (2 — self-referential pair)
- `forms/{contact-form,assessment-form,templates-pack-request-form}.tsx` (3)
- `onboarding/onboarding-panel.tsx` (1 — was already inside an `archive/` subfolder)
- `citations/{references-rail,cited-number,marker}.tsx` (3 — re-exported by barrel but never imported)

### Closed `nav/` dead cluster (12 files)

All twelve files imported each other but no live `src/app/layout.tsx` route reaches any of them. The only live `nav/` survivors are `site-header.tsx`, `site-footer.tsx`, `site-header-cta.tsx`, and `theme-toggle.tsx` (the four imported by the root layout, plus `theme-toggle` as a sibling dependency).

| File | Self-documented? |
|---|---|
| `mobile-nav.tsx` | Yes — *"⚠️ Legacy mobile drawer — not mounted in the live site"* |
| `nav-links.ts` | Yes — *"⚠️ NOT WIRED INTO THE LIVE SITE"* |
| `site-nav-desktop.tsx` | (imported only by `nav-links`) |
| `nav-link.tsx` | (no importers) |
| `nav-dropdown.tsx` | (no importers) |
| `nav-config.ts` | (imported only by dead siblings) |
| `mobile-menu.tsx` | (no importers — despite `mobile-nav`'s JSDoc claiming it was the live menu) |
| `nav-scroll-shadow.tsx` | (no importers) |
| `nav-reading-progress.tsx` | (no importers) |
| `site-nav-actions.tsx` | (no importers) |
| `site-nav-mobile-toggle.tsx` | (imported only by `site-nav-actions`) |
| `site-logo.tsx` | (imported only by `mobile-nav` + `editorial-stitch/stitch-glass-top-bar`) |

## Barrel edits

- `src/components/citations/index.ts` — removed `CitedNumber`, `Marker`, `ReferencesRail` exports.
- `src/components/editorial-stitch/index.ts` — archived (entire directory removed).

## Out of scope for this run

Naming-debt rename of `src/components/sections-mock/` (alive but misnamed) deferred to a follow-up — the right target (`sections-active/` vs folding into `sections/`) is a design call.

The `scenius-v3-topology.ts` rename inside `sections-mock/home/` is also deferred.

## How to revert this archive

Single commit; `git revert <sha>` restores everything. Renames are tracked by `git mv` so `git log --follow` continues to work on each file.
