"use client";

/**
 * @deprecated Superseded by caption-only VoiceZone (conversation choreography SSOT I1).
 * Kept for reference; no longer mounted.
 */
export function DiscussVoice(_props: {
  transcript: { role: string; content: string }[];
  multiline?: boolean;
}) {
  return null;
}
