/** Public Movemental Voice Commitments document slug + signed version string. */
export const MOVEMENT_VOICE_COMMITMENTS_SLUG = "movement-voice-commitments" as const;

/** Bump when the public commitments text changes materially (re-sign required). */
export const MOVEMENT_VOICE_COMMITMENTS_VERSION = "2026-05-11" as const;
