# Alan Hirsch — component exemplars

Isolated HTML/Tailwind **pattern** screens (heroes, cards, wildcards, etc.). They are not the primary **app shell**.

For navigation and route parity with the Alan Hirsch Next.js app (`SiteBar.tsx` + `(public)` routes), use **`../pages/`** and read **`../README.md`**.
